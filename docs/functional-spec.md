# Functional spec: skills-visionary

LOCKED pulled from `docs/vision.md`. Skill pack. Phase 0 is the
playbook itself.

## Phase 0

1. `.grok/skills/visionary/SKILL.md` — high-reasoning new-product and
   feature-request intake. Measurable success, shape, stack, architecture,
   HTML mocks when UI.
2. `.grok/skills/harvest-skills-visionary/SKILL.md` — harvest home.
3. `docs/templates/vision.md` — blank pack.
4. `docs/mocks/` — home / empty / error HTML.

## Acceptance

| id | statement |
|----|-----------|
| A1 | A new product cannot park/dispatch without shape + success LOCKED or UNKNOWN. |
| A2 | Mocks are HTML in `docs/mocks/`; no generated PNGs. |
| A3 | Feature requests run visionary before park/dispatch (same success gate; no new repo). |
| A4 | Park/create/webhook stay `bob-spec-intake` / `setup-github-webhooks` / `setup-github-cursor`. |

## UNKNOWN

- Fleet install path for this skill onto `~/.grok` (Install-BobFleet
  vs manual copy).
